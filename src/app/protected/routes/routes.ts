import { Home } from 'lucide-react';
import { NavigationItem } from '../../shared/interface/navigation';

export const protectedRoutes: NavigationItem = {
  name: 'Inicio',
  href: '/worldGaming/inicio',
  icon: Home
};